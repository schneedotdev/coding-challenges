/**
 * Name: Capitalization and Mutability
 * Description: write a simple helper function to capitalize a string (that contains a single word)
 */

function capitalizeWord(word) {
  return word[0].toUpperCase() + word.slice(1);
}